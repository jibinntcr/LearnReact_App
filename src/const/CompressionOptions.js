const thumbnailOptions = {
  maxSizeMB: 0.3, // Adjust the maximum file size in megabytes for thumbnails
  maxWidthOrHeight: 200, // Adjust the maximum width or height in pixels for thumbnails
}

export { thumbnailOptions }

const CompressionOptions = {
  thumbnailOptions: {
    maxSizeMB: 0.3, // Adjust the maximum file size in megabytes for thumbnails
    maxWidthOrHeight: 200, // Adjust the maximum width or height in pixels for thumbnails
  },
}

export default CompressionOptions
